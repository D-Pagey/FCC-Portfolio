import React, { Component } from 'react';

import './Header.css';

class Header extends Component {
  render() {
    return (
      <header class="header">
        <h1 class="title">Pagey's Portfolio</h1>
      </header>
    )
  }
}

export default Header;
